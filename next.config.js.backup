/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // Penting untuk Vercel
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
