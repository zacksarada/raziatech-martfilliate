import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/components/AuthProvider";
import Navigation from "@/components/layout/Navigation";
import Footer from "@/components/layout/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "RaziaTech Martfilliate - Affiliate Marketing Platform",
  description: "Earn commissions by promoting digital products. Join thousands of successful affiliates today.",
  keywords: "affiliate marketing, digital products, earn money online, commissions, e-commerce",
  openGraph: {
    title: "RaziaTech Martfilliate",
    description: "Earn commissions by promoting digital products",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/favicon.ico" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body className={`${inter.className} bg-gray-50`}>
        <AuthProvider>
          <Navigation />
          <main className="min-h-screen pt-16 pb-20 md:pb-0">
            {children}
          </main>
          <Footer />
        </AuthProvider>
      </body>
    </html>
  );
}
